name: sync
publish_to: none

environment:
  sdk: ">=3.0.0"

dependencies:
  core:
    path: ../core
  database:
    path: ../database
  todo:
    path: ../todo
  freezed_annotation: ^2.2.0
  freezed: ^2.3.3
  synchronized: ^3.0.0+3

dev_dependencies:
  build_runner: ^2.3.3
