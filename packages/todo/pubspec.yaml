name: todo
publish_to: none

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  core:
    path: ../core
  sync:
    path: ../sync

  freezed_annotation: ^2.2.0

dev_dependencies:
  build_runner: ^2.3.3
  freezed: ^2.3.2
