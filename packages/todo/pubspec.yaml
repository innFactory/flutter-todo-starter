name: todo
publish_to: none

environment:
  sdk: ">=2.19.6 <3.0.0"
  flutter: ">=3.7.12"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  sync:
    path: ../sync
  database:
    path: ../database
  api:
    path: ../api
  network_info:
    path: ../network_info
  app:
    path: ../app
  freezed_annotation: ^2.2.0

dev_dependencies:
  build_runner: ^2.3.3
  freezed: ^2.3.2
